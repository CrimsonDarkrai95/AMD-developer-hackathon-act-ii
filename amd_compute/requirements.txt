# Environment for the AMD Developer Cloud Jupyter notebook.
# This is intentionally separate from backend/requirements.txt: the notebook
# loads and runs a local model on the AMD GPU, which the live FastAPI backend
# never does (the live backend only calls the Featherless API over HTTPS).

torch                 # install the ROCm build appropriate for the provided AMD image
sentence-transformers
transformers
accelerate
pandas
numpy
scikit-learn
matplotlib

# --- deps for featherless_notebook_server.ipynb (the in-notebook Featherless relay) ---
fastapi
uvicorn
nest_asyncio
requests
python-dotenv
